import React from 'react'
import Title from './Title'

const TextAndDescription = (props) => {
  return (
    <>
      <Title text={props.title} type="h1"/>
    </>
  )
}

export default TextAndDescription